O programa em java faz a manipulação de arquivos .csv, o mesmo recebe como entrada de dados um arquivo.csv , contendo os seguintes dados: nome do item ,preço do item , quantidade de itens separados por virgulas , também é passado para o programa o "path" de onde o arquivo .csv se localiza, Usando a classe "BufferedReader" é feita a leitura do mesmo, usando a metodo "split" e feito a quebar da string em varias subStrings neste momento e instanciado um "ArrayList" do tipo "produto" passando os paramentros retirado da string sendo eles"nome" , "preco", "quant" depois disso é criada uma nova sub "pasta" e um novo arquivo chamado "summy.csv" para ser salvas as novas informações usando a class "BufferedWriter" o novo arquivo e subescrito contendo  a soma do preço do item vezes a quantidade.  